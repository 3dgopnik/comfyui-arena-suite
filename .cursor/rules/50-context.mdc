---
alwaysApply: true
---

# Context Management Rules

## КРИТИЧЕСКИ ВАЖНО: Обязательное чтение концепции

**В КАЖДОМ новом чате ПЕРВЫМ делом изучить концепцию Arena AutoCache** - критически важно для понимания логики работы.

## Управление концепцией

### Обязательные правила:
- **Любые изменения ТОЛЬКО с разрешения пользователя**
- Перед изменением написать что планируется изменить и зачем
- Ждать подтверждения
- Без разрешения - только чтение и анализ

## Концепция Arena AutoCache

### Реальная логика работы системы:

**Arena AutoCache v5.1.0** - система автоматического кеширования моделей ComfyUI с Settings UI и тремя режимами работы через Arena кнопку:

#### 1. **Серый режим (по умолчанию)**
- `ARENA_AUTO_CACHE_ENABLED=0`, `ARENA_AUTOCACHE_AUTOPATCH=0`
- Система полностью выключена
- Использование оригинальных путей к моделям

#### 2. **Красный режим (активное кеширование)**
- `ARENA_AUTO_CACHE_ENABLED=1`, `ARENA_AUTOCACHE_AUTOPATCH=1`
- Кеширование новых моделей + использование кеша
- Патчинг `folder_paths.get_full_path()` для перехвата загрузки
- Фоновое копирование в отдельном потоке

#### 3. **Зеленый режим (только использование)**
- `ARENA_AUTO_CACHE_ENABLED=1`, `ARENA_AUTOCACHE_AUTOPATCH=0`
- Использует кеш, НЕ кеширует новые модели
- Индикатор некешированных моделей в тултипе
- Клик на индикатор переводит в красный режим

### Способы настройки AutoCache:

**1. Через ComfyUI Settings UI (основной способ):**
- Settings → arena → кнопка **💾 Save to .env** (вверху)
- Заполнить **Cache Root** и другие настройки
- Нажать Save → создается `user/arena_autocache.env` с **ENABLED=0, AUTOPATCH=0**
- Система остается выключенной до активации через Arena кнопку

**2. Через Arena кнопку (управление режимами):**
- **Серый** (по умолчанию): система выключена
- **Красный**: клик на кнопку → активное кеширование
- **Зеленый**: dropdown меню → только использование кеша
- **Dropdown меню**: выбор любого режима

**3. Через ноду на canvas (альтернативный):**
- Добавить Arena AutoCache ноду
- Включить `enable_caching=True`
- Настроить → записываются в .env

### Приоритеты настроек:

**Активная нода (enable_caching=True):**
- Настройки из ноды перезаписывают .env
- Позволяет разные пути для разных workflow

**Settings UI:**
- Глобальные настройки для всех workflow
- Создается/обновляется .env файл
- Чтение через DOM + fallback для Cache Root

**При загрузке модели:**
- Проверка наличия в кеше (SSD)
- Cache hit → использует из кеша
- Cache miss → копирует с NAS в фоне
- Возвращает оригинальный путь для загрузки

### Технические детали:

**Поддерживаемые категории моделей:**
- `checkpoints`, `loras`, `clip`, `vae`, `controlnet`
- `upscale_models`, `embeddings`, `hypernetworks`
- `gguf_models`, `unet_models`, `diffusion_models`

**Настройки через .env файл:**
- `ARENA_CACHE_ROOT` - путь к кешу (SSD)
- `ARENA_CACHE_MIN_SIZE_MB` - минимальный размер файла
- `ARENA_CACHE_MAX_GB` - лимит кеша (0 = без лимита)
- `ARENA_CACHE_MODE` - режим (ondemand/eager/disabled)
- `ARENA_AUTO_CACHE_ENABLED` - включение авто-кеширования (1/0)
- `ARENA_AUTOCACHE_AUTOPATCH` - автопатч при загрузке (1/0)
- `ARENA_CACHE_VERBOSE` - подробное логирование (1/0)
- `ARENA_CACHE_DISCOVERY` - режим обнаружения (workflow_only/all_models)
- `ARENA_CACHE_PREFETCH_STRATEGY` - стратегия предзагрузки (lazy/eager)
- `ARENA_CACHE_MAX_CONCURRENCY` - макс. параллельных операций
- `ARENA_CACHE_SESSION_BYTE_BUDGET` - лимит байтов за сессию
- `ARENA_CACHE_COOLDOWN_MS` - задержка между операциями

**Settings UI (arena_settings_save_button.js):**
- Регистрация через `app.ui.settings.addSetting`
- Чтение значений: DOM query + fallback сканирование text inputs
- Кнопка Save (ID: `arena.aaa_save_button`) вверху списка
- POST `/arena/env` для сохранения настроек
- **ВСЕГДА создает .env с ENABLED=0, AUTOPATCH=0** (безопасный режим)

**Arena кнопка (arena_simple_header.js):**
- Split button с dropdown меню
- Три режима: серый/красный/зеленый
- API endpoint `/arena/uncached_models` для подсчета некешированных
- Автоматическое определение режима при загрузке

**Безопасность:**
- По умолчанию кеширование ОТКЛЮЧЕНО
- Проверка глубины путей
- Валидация настроек из .env
- Thread-safe операции с блокировками

### Статус разработки:

- ✅ **v5.1.0** - Три режима Arena кнопки с dropdown меню
- ✅ **Settings UI** - кнопка Save to .env (ENABLED=0, AUTOPATCH=0)
- ✅ **OnDemand режим** - реализован
- ✅ **Deferred autopatch** - глобальное кеширование
- ✅ **Поддержка .env** - полная интеграция
- ✅ **Безопасность** - проверки и валидация
- ✅ **Issue #116 ЗАКРЫТ** - Settings UI реализован
- ✅ **Issue #147** - Три режима Arena кнопки

### Закрытые Issues:

**#116** - Settings UI с кнопкой Save ✅
- Реализована кнопка **💾 Save to .env** в Settings → arena
- Чтение настроек из DOM с fallback
- Создание .env файла через backend endpoint

**#147** - Три режима Arena кнопки ✅
- Реализованы серый/красный/зеленый режимы
- Dropdown меню для переключения
- Индикатор некешированных моделей в зеленом режиме
- API endpoint `/arena/uncached_models`
- Дефолтные значения ENABLED=0, AUTOPATCH=0 в Settings UI

**#160** - Порядок переключения режимов Arena кнопки ✅
- Исправлен порядок: GRAY→GREEN→RED→GRAY
- Первый клик: серый → зеленый (используем кеш)
- Второй клик: зеленый → красный (кешируем)
- Третий клик: красный → серый (выключено)

**#161** - GREEN режим использует кеш ✅
- Frontend: autopatch вызывается для GREEN и RED режимов
- Backend: улучшена логика patched_get_full_path
- Cache HIT: возвращает путь из кеша (оба режима)
- Cache MISS GREEN: использует NAS без копирования
- Cache MISS RED: копирует в фоне + использует NAS
- Добавлено verbose логирование для всех сценариев

### Открытые Issues:

**#113** - Deferred autopatch диагностика
**#112** - Auto-extend категорий в .env
**#110** - Автоопределение моделей из workflow

### Последние изменения (2025-10-10):

- **✅ GREEN режим теперь использует кеш** - исправлена логика работы (#161)
  - Frontend: autopatch вызывается для GREEN и RED режимов
  - Backend: улучшена логика patched_get_full_path с проверкой AUTOPATCH флага
  - Cache HIT → используется кеш (SSD) в обоих режимах
  - Cache MISS GREEN → используется NAS (без копирования)
  - Cache MISS RED → копируется в фон + используется NAS
  - Добавлены verbose логи: ✅ Cache HIT, ⏳ Cache MISS (RED), ⏭️ Cache MISS (GREEN)
- **✅ Исправлен порядок переключения режимов** - GRAY→GREEN→RED→GRAY (#160)
  - Первый клик (серый): → зеленый (используем кеш, не кешируем)
  - Второй клик (зеленый): → красный (кешируем и используем)
  - Третий клик (красный): → серый (выключено)
- **✅ Три режима Arena кнопки** - серый/красный/зеленый с dropdown меню
- **✅ Дефолтные значения 0/0** - Settings UI создает .env с ENABLED=0, AUTOPATCH=0
- **✅ API endpoint** - `/arena/uncached_models` для подсчета некешированных
- **✅ Индикатор в тултипе** - зеленый режим показывает количество некешированных моделей
- **✅ Dropdown меню** - переключение между режимами через стрелку
- **✅ Автоопределение режима** - при загрузке читает состояние из API
- Инициатор: пользователь
- Влияние: интуитивное управление кешированием через UI

## Структура проекта:

**Активные JS файлы:**
- `web/arena_settings_save_button.js` - Settings UI
- `web/arena_autocache.js` - основная логика
- `web/arena_simple_header.js` - кнопки в header
- `web/arena_workflow_analyzer.js` - анализ workflow

**Скрипты:**
- `scripts/sync_js_files.ps1` - синхронизация JS в Electron
- `scripts/ARENA_MANIFEST.example.yaml` - пример манифеста

## Что НЕ требует разрешения:
- Чтение и анализ документации
- Обновление "Последние изменения" после согласованных изменений
- Исправление опечаток

## Что ТРЕБУЕТ разрешения:
- Изменения в логике работы
- Добавление новых состояний
- Изменение концепции .env файлов
- Модификация workflow
